# ============================================
# MongoDB Configuration
# ============================================
# Production MongoDB connection string (for production/staging)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/lpai?retryWrites=true&w=majority

# Local MongoDB connection string (for development)
# Used when NODE_ENV=development
# Format: mongodb://localhost:27017/database
LOCAL_MONGODB_URI=mongodb://localhost:27017/local

# ============================================
# Security & Authentication
# ============================================
# JWT Secret - Minimum 32 characters required for token signing
# Generate a strong random secret for production
JWT_SECRET=your-super-secret-key-minimum-32-characters-long-for-jwt-signing

# Encryption Key - 64 character hex string for API key encryption
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-64-character-hex-string-for-api-key-encryption-make-it-long-and-secure

# ============================================
# Cloudflare R2 Storage (for PDFs and files)
# ============================================
# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=your-r2-bucket-name

# Optional: Custom public URL for R2 bucket (if using custom domain)
# R2_PUBLIC_URL=https://your-custom-domain.com

# ============================================
# AWS S3 Storage (alternative to R2)
# ============================================
# AWS credentials from AWS IAM console
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS region where your S3 bucket is located
AWS_REGION=us-east-1

# S3 bucket name for file storage
AWS_S3_BUCKET_NAME=your-s3-bucket-name

# ============================================
# Ably (Real-time Webhooks & Messaging)
# ============================================
# Get from Ably Dashboard > Account Settings > API Keys
ABLY_API_KEY=your-ably-api-key-here

# ============================================
# Email Service (Resend)
# ============================================
# Get from Resend Dashboard > API Keys
RESEND_API_KEY=re_your_resend_api_key_here

# Admin email for system notifications
ADMIN_EMAIL=admin@yourdomain.com

# ============================================
# Stripe (Payment Processing)
# ============================================
# Get from Stripe Dashboard > Developers > API Keys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe webhook secret for verifying webhook signatures
# Get from Stripe Dashboard > Developers > Webhooks > Add endpoint
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret

# ============================================
# Radar (Location Services - Optional)
# ============================================
# RADAR_SECRET_KEY=your_radar_secret_key

# ============================================
# Application URLs
# ============================================
# Public API URL (used in webhooks and external calls)
# For local: http://localhost:3000
# For production: https://your-api-domain.com
NEXT_PUBLIC_API_URL=http://localhost:3000

# Public App URL (frontend URL)
# For local: http://localhost:3000
# For production: https://your-app-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Environment Configuration
# ============================================
# Node environment: development, staging, or production
NODE_ENV=development

# Server port (optional, defaults to 3000 for Next.js)
PORT=3000

# ============================================
# Vercel Deployment (if using Vercel)
# ============================================
# VERCEL_REGION=iad1

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file to .env.local for local development:
#    cp env.example .env.local
#
# 2. Next.js automatically loads .env.local files
#
# 3. For production, set these variables in your hosting platform
#    (Vercel, AWS, etc.) environment variables section
#
# 4. NEVER commit .env.local or .env files to version control
#    They contain sensitive information (already in .gitignore)
#
# 5. Required variables for basic functionality:
#    - MONGODB_URI (or LOCAL_MONGODB_URI for dev)
#    - JWT_SECRET
#
# 6. Optional but recommended:
#    - R2_* variables (for file storage)
#    - ABLY_API_KEY (for real-time features)
#    - RESEND_API_KEY (for email)
#    - STRIPE_* variables (for payments)
#
# 7. MongoDB Connection String Format:
#    - Atlas (Cloud): mongodb+srv://user:pass@cluster.mongodb.net/dbname
#    - Local: mongodb://localhost:27017/dbname
#    - With auth: mongodb://user:pass@host:27017/dbname
#    - With options: mongodb://host:27017/dbname?retryWrites=true&w=majority
